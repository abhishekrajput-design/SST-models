# ===================================
# Multi-Speaker Call Processor
# Requirements — Python 3.10+
# ===================================

# Core ML frameworks
# Install with CUDA: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.8.0
torchaudio>=2.8.0

# Speaker diarization — 4.0 uses VBx clustering + exclusive mode (~28% better DER)
pyannote.audio>=4.0.0

# Speaker recognition (ECAPA-TDNN — kept as fallback)
speechbrain>=1.0.0

# WeSpeaker CAM++ — 512-dim embeddings, EER 0.62% (vs ECAPA 0.93%)
# pip install git+https://github.com/wenet-e2e/wespeaker.git
# wespeaker @ git+https://github.com/wenet-e2e/wespeaker.git

# Clustering (KMeans fallback when TS-VAD not available)
scikit-learn>=1.3.0

# Fast Whisper transcription (CTranslate2-based, int8 support)
faster-whisper>=1.0.0

# Audio processing
soundfile>=0.12.0
numpy<2.0
numba>=0.59.0         # pin before librosa — older numba breaks on Python 3.12
librosa>=0.10.0       # desk-recording cleanup pipeline (angelina)
noisereduce>=3.0.0    # two-pass spectral gating (used by angelina)

# Utilities
huggingface_hub>=0.20.0
requests>=2.31.0
tqdm>=4.66.0
tabulate>=0.9.0
